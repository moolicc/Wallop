<UserControl x:Class="WallApp.UI.Views.TrayIconToolTip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WallApp.UI.Views"
             xmlns:ui="clr-namespace:WallApp.UI"
             mc:Ignorable="d" MinWidth="300">
    <UserControl.Resources>
        <ui:IconFileSizeSelectorConverter x:Key="IconSelector" />
    </UserControl.Resources>
    <Border BorderThickness="2" CornerRadius="4">
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Image x:Name="_image" x:FieldModifier="private" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Margin="6" Width="64" Height="64" />

            <TextBlock x:Name="_titleBlock" x:FieldModifier="private" Grid.Row="0" Grid.Column="1" FontSize="24" FlowDirection="RightToLeft" />
            <TextBlock x:Name="_textBlock" x:FieldModifier="private" Grid.Row="1" Grid.Column="1" FontSize="14" FlowDirection="RightToLeft" />

        </Grid>
    </Border>
</UserControl>
